# 폰 검수 프로그램 — 빌드 & 배포 안내

프로그램 폴더: C:\phone_inspector (Windows 개발 기준)

⚠️ 중요: 빌드는 각 OS의 실제 컴퓨터에서만 됩니다.
  - Windows exe → Windows PC (Visual Studio 필요, 이미 있음)
  - macOS 앱   → 실제 Mac (Xcode 필요)
크로스 빌드(윈도우에서 맥용 빌드)는 불가능합니다.

═══════════════════════════════════════════════
■ 1부. Windows — exe 설치 마법사 만들기
═══════════════════════════════════════════════

── 1단계. 릴리스 빌드 ──
개발용(flutter run)이 아니라 배포용 빌드를 만듭니다.

  cd C:\phone_inspector
  flutter build windows --release

결과물 위치:
  C:\phone_inspector\build\windows\x64\runner\Release\
이 폴더 안에 phone_inspector.exe 와 여러 dll, data 폴더가 생깁니다.
⚠️ 이 폴더 통째로가 프로그램입니다. exe 하나만 복사하면 안 됩니다.

── 2단계. adb 동봉 준비 ──
검수 PC마다 adb 설치를 시키지 않으려면 adb를 프로그램에 넣습니다.

1) platform-tools 폴더(adb.exe, AdbWinApi.dll, AdbWinUsbApi.dll 포함)를
   위 Release 폴더 안에 'platform-tools' 라는 이름으로 복사.
   (본인 PC의 adb 위치는 보통:
    C:\Users\yiha7\AppData\Local\Android\Sdk\platform-tools )

2) 코드에서 adb 경로를 동봉된 것으로 바꾸기 (배포판용).
   adb_auto_diagnostics.dart 와 main.dart 의 'adb' 를
   실행파일 옆 platform-tools\adb.exe 로 가리키게 하려면,
   아래 헬퍼를 main.dart 에 넣고 adbPath 로 넘기세요:

   import 'dart:io';
   String resolveAdbPath() {
     final exeDir = File(Platform.resolvedExecutable).parent.path;
     final bundled = '$exeDir\\platform-tools\\adb.exe';
     return File(bundled).existsSync() ? bundled : 'adb';
   }
   // 사용: AdbAutoDiagnostics(adbPath: resolveAdbPath())

   (동봉 adb 가 없으면 시스템 adb 로 자동 폴백)

── 3단계. Inno Setup 설치 ──
무료 설치 마법사 제작 툴.
  https://jrsoftware.org/isdl.php  에서 다운로드 후 설치.

── 4단계. 설치 스크립트(.iss) 작성 ──
아래 내용을 installer.iss 로 저장 (경로/이름은 본인에 맞게):

  [Setup]
  AppName=폰 검수
  AppVersion=1.0
  DefaultDirName={autopf}\PhoneInspector
  DefaultGroupName=폰 검수
  OutputBaseFilename=PhoneInspector_Setup
  Compression=lzma2
  SolidCompression=yes
  ArchitecturesInstallIn64BitMode=x64

  [Files]
  ; Release 폴더 전체를 통째로 포함 (platform-tools 포함)
  Source: "C:\phone_inspector\build\windows\x64\runner\Release\*"; \
    DestDir: "{app}"; Flags: recursesubdirs createallsubdirs

  [Icons]
  Name: "{group}\폰 검수"; Filename: "{app}\phone_inspector.exe"
  Name: "{commondesktop}\폰 검수"; Filename: "{app}\phone_inspector.exe"

  [Run]
  Filename: "{app}\phone_inspector.exe"; Description: "실행"; \
    Flags: nowait postinstall skipifsilent

── 5단계. 컴파일 ──
Inno Setup 에서 installer.iss 열고 → Build → Compile (또는 F9).
Output 폴더에 PhoneInspector_Setup.exe 생성됨.
이게 검수 PC에 배포할 설치 마법사입니다. 더블클릭하면 설치 진행.

═══════════════════════════════════════════════
■ 2부. macOS — 맥북용 앱 만들기 (회사 시연용)
═══════════════════════════════════════════════

⚠️ 실제 Mac + Xcode 가 필요합니다. Windows 에선 불가능.

── 1단계. Mac 에 개발환경 준비 ──
  1) Xcode 설치 (App Store)
  2) Flutter 설치:
     https://docs.flutter.dev/get-started/install/macos
  3) 확인:  flutter doctor
     (macOS desktop, Xcode 초록불 확인)

── 2단계. 프로젝트 옮기기 ──
C:\phone_inspector 폴더 전체를 Mac 으로 복사 (USB, 깃, 클라우드 등).
Mac 에서 터미널로 그 폴더 진입.

── 3단계. macOS 데스크톱 활성화 & 빌드 ──
  flutter config --enable-macos-desktop
  flutter create --platforms=macos .    # macos 폴더가 없으면 생성
  flutter pub get
  flutter run -d macos                   # 먼저 실행 테스트

── 4단계. adb (Mac 용) ──
Mac 엔 adb 가 기본으로 없으니:
  brew install android-platform-tools
그러면 'adb' 명령이 시스템에 잡혀서, 코드의 adbPath: 'adb' 가 그대로 작동.
(맥 배포판에도 adb 를 동봉하려면 Homebrew 대신 platform-tools 바이너리를
 앱 번들에 넣고 경로 처리 — 시연만이면 brew 로 충분)

── 5단계. 릴리스 빌드 & 배포 ──
  flutter build macos --release
결과물:
  build/macos/Build/Products/Release/phone_inspector.app
이 .app 파일이 맥용 프로그램. 더블클릭하면 실행.
시연이면 이 .app 을 맥북에 복사해서 실행하면 됩니다.

⚠️ 서명 안 된 앱은 첫 실행 시 "확인되지 않은 개발자" 경고가 뜹니다.
   우클릭 → 열기 → 열기 로 우회 가능 (시연엔 충분).
   정식 배포는 Apple Developer 계정으로 코드 서명 필요.

═══════════════════════════════════════════════
■ 요약
═══════════════════════════════════════════════
Windows: flutter build windows --release → Release폴더에 adb 동봉 →
         Inno Setup 으로 PhoneInspector_Setup.exe 생성
macOS:   Mac에서 flutter build macos --release → .app 파일 →
         brew로 adb 설치 → 더블클릭 실행

두 빌드 모두 소스코드(lib 폴더)는 동일합니다. UI·검사 로직은 그대로,
플랫폼별 빌드 명령과 adb 경로 처리만 다릅니다.
