# 폰 검수 프로그램 — 프로젝트 인수인계 (Claude Code용)

이 문서는 chat에서 진행하던 작업을 Claude Code로 이어가기 위한 인수인계입니다.
Claude Code는 이 문서를 먼저 읽고 현재 상태를 파악한 뒤 이어서 작업하세요.

═══════════════════════════════════════════
■ 프로젝트가 뭔가
═══════════════════════════════════════════
중고폰 매입·판매 회사의 **휴대폰 검수(grading) 자동화 프로그램**.
사용자는 이 회사에 취업했고, 검수를 자동화하려고 만드는 중.
PC(Flutter Desktop) 프로그램에서 USB로 연결된 안드로이드 폰을 adb로 검사하고,
결과를 대시보드 카드로 보여줌. 대량 검수를 위해 USB 허브로 여러 대 동시 처리가 목표.

═══════════════════════════════════════════
■ 확정된 핵심 결정들 (중요 — 되돌리지 말 것)
═══════════════════════════════════════════
1. **폰에 앱 설치 안 함.** 매입한 남의 폰마다 앱 설치·삭제가 부담이라 폐기.
   모든 검사는 PC의 adb(dumpsys/getprop 등)로 처리. (폰 앱 코드는 폐기됨)

2. **검사 = adb 자동 + 육안 수동, 두 종류.**
   - adb 자동 (프로그램이 판정): 배터리, SIM, 무선(WiFi·BT·NFC), 센서, 저장소, 시스템 소프트웨어
   - 육안 (검수원이 폰 실물 보고 대시보드에 정상/불량 입력): 액정(데드픽셀·번인),
     카메라 렌즈, 지문, 스피커·마이크 소리, S펜(해당 기종만)

3. **IMEI·락/FRP 자동추출은 안 함.** 최신 삼성폰(S25 울트라)에서 adb IMEI 추출이
   다 막힌 걸 실기 확인. 둘 다 어차피 육안 확인 항목이라 제외.

4. **무선 검사는 실제 통신 검증까지.** WiFi는 켜짐이 아니라 실제 AP연결+핑(8.8.8.8)까지.
   검수장에 인터넷 되는 테스트용 와이파이가 상시 있음. (폐쇄망이면 게이트웨이로 핑 대상 변경 필요)

5. **저장소는 전체 용량만** 표시 (여유 용량 X).

6. **배포**: Windows exe 설치 마법사(Inno Setup, adb 동봉) + macOS 앱(회사 시연이 맥북).
   빌드는 각 OS 실기에서만. GitHub Actions 자동 빌드 구성 중.

═══════════════════════════════════════════
■ 실기 검증 완료 (S25 울트라 SM-S938N, 시리얼 R3KYC05DHRL)
═══════════════════════════════════════════
adb 자동 검사 6종이 실제 폰에서 정상 동작 확인됨:
- 배터리: 잔량·온도(39.2°C)·health(Good)·충전소스(USB) ✓
- SIM: 삽입됨·통신사(KT) ✓
- 무선: WiFi 실제 핑 통신 정상·BT·NFC ✓
- 센서: 하드웨어 40개 전부 정상, 필수센서(가속도·자이로·지자기·조도·근접) 다 존재 ✓
- 저장소: 전체 용량 ✓
- 시스템: 모델·Android 16·SDK 36·One UI 8.5 ✓

═══════════════════════════════════════════
■ 현재 코드 상태 (이 repo)
═══════════════════════════════════════════
lib/main.dart
  - Flutter Desktop 대시보드. 기기 카드 그리드, 검사 시작 버튼, 결과 표시.
  - resolveAdbPath(): 설치본은 실행파일 옆 platform-tools\adb, 개발 중엔 시스템 adb.
  - 센서 항목에 (i) 상세보기 버튼 → 다이얼로그로 센서 전체 목록 표시.
  - _statusBadge: 미검사/통과/불량 배지.

lib/adb_auto_diagnostics.dart
  - adb 자동 검사 엔진. runAll(serial)이 6종 검사 실행.
  - battery / sim / wireless / sensors / storage / systemSoftware 메서드.
  - 각 메서드는 {test, status, pass, data, message} Map 반환.
  - wireless는 _wifiCheck(핑까지)/_bluetoothCheck/_nfcCheck 로 분리, 실제 기능 검증.

.github/workflows/build.yml
  - GitHub Actions: 태그(v*) 푸시 시 Windows 빌드 → adb 다운로드 동봉 →
    Inno Setup 컴파일 → 설치 마법사를 릴리스에 자동 첨부.

installer/installer.iss
  - Inno Setup 스크립트 (한글, adb 포함, 상대경로로 CI 대응).

═══════════════════════════════════════════
■ 아직 안 된 것 / 다음 할 일 (우선순위 순)
═══════════════════════════════════════════
1. **한국어 문구 다듬기** — 사용자가 번역 어색한 부분 고쳐달라고 함.
   검수 현장 말투로. (예: health "Good"→"정상", "SIM 삽입됨"→"유심 정상" 등)
   ※ 사용자에게 구체적으로 어디가 어색한지 확인 후 진행 권장.

2. **육안 판정 UI** — 폰 앱을 대체하는 핵심.
   대시보드 카드에 액정·카메라·지문·스피커·마이크·S펜을 검수원이 정상/불량 클릭.
   adb 보조 버튼도: 단색화면 띄우기(액정 검수용), 소리 재생(스피커, 사이렌음),
   진동 울리기(cmd vibrator), 볼륨버튼 감지(getevent).
   ※ 스피커는 삼성 *#0*# 처럼 명확한 사이렌/경고음 재생 방식 원함.

3. **S펜 기종 자동 판별** — 모델명으로 Note/Ultra 계열 판별해서 해당 폰에만
   S펜 육안 항목 표시. S펜은 센서 아님(디지타이저). 필압·그리기는 육안.

4. **대량 처리** — USB 허브로 여러 대 동시. 폰별 시리얼 구분.
   (chat 초반에 device_orchestrator 만들었으나 그건 WebSocket 기반 구버전,
    지금 adb 전용 방향에 맞게 재작성 필요)

5. **아이폰** ("되면 보너스") — adb 안 됨. libimobiledevice(ideviceinfo 등) 사용.
   시스템 정보·배터리 정도만 자동, 나머진 육안. 육안 UI는 안드로이드와 공용.

6. **exe 설치 마법사 / macOS 빌드** — GitHub Actions 설정은 됨.
   사용자가 깃헙에 올리고 태그 푸시하면 자동 빌드. macOS는 Mac 실기 필요.

═══════════════════════════════════════════
■ 개발 환경
═══════════════════════════════════════════
- Windows PC (사용자명 yiha7), Flutter 3.44.8 / Dart 3.12.2
- Android SDK·adb 기존 설치됨 (PATH에 platform-tools 있음)
- Visual Studio Community 2022 (Windows 데스크톱 빌드용) 있음, flutter doctor 올그린
- 개발 중이던 PC 앱 폴더: C:\phone_inspector
- 테스트 폰: 갤럭시 S25 울트라 (본인 폰)

═══════════════════════════════════════════
■ Claude Code가 처음 할 일
═══════════════════════════════════════════
1. flutter create 로 이 repo를 정식 Flutter 프로젝트로 만들거나,
   기존 C:\phone_inspector에 이 lib/ 파일들을 넣기.
2. pubspec.yaml 확인 (지금 자동 검사만 쓰면 외부 패키지 거의 불필요).
3. flutter run -d windows 로 현재 상태 실행 확인.
4. 위 "다음 할 일" 1번(한국어)부터, 사용자와 상의하며 진행.
